service: test-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x

functions:
  api:
    handler: handler.handler
    events:
      - httpApi: '*'
      - s3:
         bucket: html-corrector-bucket-1
         event: s3:ObjectCreated:*
